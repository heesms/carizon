<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.carizon.api.mapper.CarsMapper">

    <sql id="searchConditions">
        <where>
            <if test="maker != null and maker != ''">
                AND MAKER_CODE = #{maker}
            </if>
            <if test="modelGroup != null and modelGroup != ''">
                AND MODEL_GROUP_CODE = #{modelGroup}
            </if>
            <if test="model != null and model != ''">
                AND MODEL_CODE = #{model}
            </if>
            <if test="trim != null and trim != ''">
                AND TRIM_CODE = #{trim}
            </if>
            <if test="grade != null and grade != ''">
                AND GRADE_CODE = #{grade}
            </if>
            <if test="q != null and q != ''">
                AND (
                    MAKER_NAME LIKE CONCAT('%', #{q}, '%')
                    OR MODEL_GROUP_NAME LIKE CONCAT('%', #{q}, '%')
                    OR MODEL_NAME LIKE CONCAT('%', #{q}, '%')
                    OR TRIM_NAME LIKE CONCAT('%', #{q}, '%')
                    OR GRADE_NAME LIKE CONCAT('%', #{q}, '%')
                    OR CAR_NO LIKE CONCAT('%', #{q}, '%')
                )
            </if>
        </where>
    </sql>

    <select id="searchCars" resultType="com.carizon.api.dto.CarListItem">
        SELECT
            PLATFORM_CAR_ID AS platformCarId,
            PLATFORM_NAME AS platformName,
            PLATFORM_CAR_KEY AS platformCarKey,
            CAR_NO AS carNo,
            CAR_ID AS carId,
            MAKER_CODE AS makerCode,
            MODEL_GROUP_CODE AS modelGroupCode,
            MODEL_CODE AS modelCode,
            TRIM_CODE AS trimCode,
            GRADE_CODE AS gradeCode,
            MAKER_NAME AS makerName,
            MODEL_GROUP_NAME AS modelGroupName,
            MODEL_NAME AS modelName,
            TRIM_NAME AS trimName,
            GRADE_NAME AS gradeName,
            PRICE AS price,
            KM AS km,
            YYMM AS yymm,
            STATUS AS status,
            COLOR AS color,
            FUEL AS fuel,
            transmission AS transmission,
            BODY_TYPE AS bodyType,
            M_URL AS mUrl,
            PC_URL AS pcUrl,
            FIRST_AD_DAY AS firstAdDay,
            CREATED_AT AS createdAt,
            UPDATED_AT AS updatedAt,
            extra AS extra,
            last_seen_date AS lastSeenDate,
            region AS region,
            displacement AS displacement
        FROM platform_car
        <include refid="searchConditions"/>
        ORDER BY UPDATED_AT DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countCars" resultType="int">
        SELECT COUNT(*)
        FROM platform_car
        <include refid="searchConditions"/>
    </select>

    <select id="getCarDetailById" resultType="com.carizon.api.dto.CarDetailDto">
        SELECT
            CAR_ID AS carId,
            CAR_NO AS carNo,
            MAKER_CODE AS makerCode,
            MODEL_GROUP_CODE AS modelGroupCode,
            MODEL_CODE AS modelCode,
            TRIM_CODE AS trimCode,
            GRADE_CODE AS gradeCode,
            MAKER_NAME AS makerName,
            MODEL_GROUP_NAME AS modelGroupName,
            MODEL_NAME AS modelName,
            TRIM_NAME AS trimName,
            YEAR AS year,
            MILEAGE AS mileage,
            COLOR AS color,
            transmission AS transmission,
            FUEL AS fuel,
            displacement AS displacement,
            BODY_TYPE AS bodyType,
            region AS region,
            adv_status AS advStatus,
            last_seen_date AS lastSeenDate,
            CREATED_AT AS createdAt,
            UPDATED_AT AS updatedAt
        FROM car_master
        WHERE CAR_ID = #{carId}
    </select>

    <select id="getPlatformCarsByCarId" resultType="com.carizon.api.dto.PlatformListingDto">
        SELECT
            PLATFORM_CAR_ID AS platformCarId,
            PLATFORM_NAME AS platformName,
            PLATFORM_CAR_KEY AS platformCarKey,
            PRICE AS price,
            KM AS km,
            STATUS AS status,
            M_URL AS mUrl,
            PC_URL AS pcUrl,
            FIRST_AD_DAY AS firstAdDay,
            last_seen_date AS lastSeenDate
        FROM platform_car
        WHERE CAR_ID = #{carId}
        ORDER BY UPDATED_AT DESC
    </select>

</mapper>

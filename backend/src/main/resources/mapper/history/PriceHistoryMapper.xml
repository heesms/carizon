<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.carizon.api.mapper.PriceHistoryMapper">

    <select id="getPriceHistoryByPlatformCarId" resultType="com.carizon.api.dto.PriceHistoryPointDto">
        SELECT
            h.checked_at AS checkedAt,
            h.price AS price,
            pc.platform_name AS platformName
        FROM car_price_history h
        JOIN platform_car pc ON h.platform_car_id = pc.platform_car_id
        WHERE h.platform_car_id = #{platformCarId}
        ORDER BY h.checked_at ASC
    </select>

    <select id="getPriceHistoryByCarId" resultType="com.carizon.api.dto.PriceHistoryPointDto">
        SELECT
            h.checked_at AS checkedAt,
            h.price AS price,
            pc.platform_name AS platformName
        FROM car_price_history h
        JOIN platform_car pc ON h.platform_car_id = pc.platform_car_id
        WHERE pc.car_id = #{carId}
        ORDER BY h.checked_at ASC
    </select>

</mapper>

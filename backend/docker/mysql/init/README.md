# MySQL 초기 스크립트

이 디렉토리에 `.sql` 파일을 넣으면 MySQL 컨테이너가 처음 시작될 때 자동으로 실행됩니다.

## 사용 방법

1. DDL 스크립트를 `.sql` 파일로 이 디렉토리에 저장
2. `docker-compose up -d mysql` 실행
3. 스크립트가 자동으로 실행됨

## 주의사항

- 이미 데이터베이스가 생성되어 있다면 이 스크립트는 실행되지 않습니다
- 기존 데이터를 유지하려면 볼륨(`mysql_data`)을 그대로 사용하세요
